{
  "name": "showdown-katex-extension",
  "homepage": "https://github.com/knguyen0125/showdown-katex-extension",
  "version": "0.1.1",
  "description": "KaTeX extension for ShowdownJS",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "clean": "rm -rf ./dist",
    "test": "jest",
    "build": "tsc",
    "prepublishOnly": "npm test",
    "prepare": "npm run clean && npm run build",
    "postversion": "git push && git push --tags"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/knguyen0125/showdown-katex-extension"
  },
  "author": "Kevin Nguyen",
  "license": "MIT",
  "dependencies": {
    "katex": "^0.10.0"
  },
  "peerDependencies": {
    "showdown": "^1.9.0"
  },
  "devDependencies": {
    "@types/jest": "^23.3.12",
    "@types/showdown": "^1.9.2",
    "jest": "^23.6.0",
    "showdown": "^1.9.0",
    "ts-jest": "^23.10.5",
    "typescript": "^3.2.2"
  }
}
